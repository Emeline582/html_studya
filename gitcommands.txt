git =checks if git is installed in the laptop -a

git init- makes the folder to start using git.DONE ONLY ONCE.

git add . = Adds all files in the folder to be tracked by git .

git commit -m "first commit" = Adds a message/ tag to your changes.

git config --global user.email "youemailusedfromgithub". DONE ONCE.

git config --global user.name "yourgithubusername". DONE ONCE.

git remote add origin git remote add origin https://github.com/Emeline582/html_study.git =
connects the folder in your computer to the github repo. DONE ONCE.

git push origin master - pushes code from comp to github repo.



